using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Battle : MonoBehaviour
{
    //Character c;// get from CharacterSelect script - public selectedCharacter
    //boolean isFIghting = false;
    // Start is called before the first frame update
    void Start() // OnButtonPress()
    {
        //c = GetComponentInParent<CharacterSelect>().selectedCharacter;
        //Vector2 imageDimensions = this.getComponent<ImageTarget>().GetSize();
        //float x = imageDimensions.x;
        //float y = imageDimensions.y;
        //this.transform.position = new Vector3(x/2, this.transform.position.y, y/2);
        //c.transform.position = new Vector3(x + x/2, c.transform.position.y, y + y/2);
        //isFighting = true;
    }

    // Update is called once per frame
    void Update()
    {
        //if (isFighting) {
            //Time t = now
            // if (t == t+.5 seconds) {
            //  enemyAttack
            //}
        //}
    }
}
